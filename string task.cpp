#include <bits/stdc++.h>
using namespace std;
main () {
string a;
    cin >> a;
    int c = 0;
    int b = 0;
    for (int i = 0; i < a.size() - 1; i++)
    {
        if (a[i] == 'a' or a[i] == 'e' or a[i] == 'i' or a[i] == 'o' or a[i] == 'u')
        {

    cout <<"."<<a;
        }
    }

}
